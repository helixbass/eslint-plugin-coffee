// Generated by CoffeeScript 2.3.1
(function() {
  'use strict';
  var assert, fs, path, plugin, ruleFiles;

  plugin = require('..');

  assert = require('assert');

  fs = require('fs');

  path = require('path');

  ruleFiles = fs.readdirSync(path.resolve(__dirname, '../rules/')).map((f) => {
    return path.basename(f, '.js');
  });

  describe('all rule files should be exported by the plugin', function() {
    return ruleFiles.forEach(function(ruleName) {
      return it(`should export ${ruleName}`, function() {
        return assert.equal(plugin.rules[ruleName], require(path.join('../rules', ruleName)));
      });
    });
  });

}).call(this);
