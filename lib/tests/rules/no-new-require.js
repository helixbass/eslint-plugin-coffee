// Generated by CoffeeScript 2.5.0
(function() {
  /**
   * @fileoverview Tests for no-new-require rule.
   * @author Wil Moore III
   */
  'use strict';
  var RuleTester, path, rule, ruleTester;

  //------------------------------------------------------------------------------
  // Requirements
  //------------------------------------------------------------------------------
  rule = require('eslint/lib/rules/no-new-require');

  ({RuleTester} = require('eslint'));

  path = require('path');

  //------------------------------------------------------------------------------
  // Tests
  //------------------------------------------------------------------------------
  ruleTester = new RuleTester({
    parser: path.join(__dirname, '../../..')
  });

  ruleTester.run('no-new-require', rule, {
    valid: ["appHeader = require('app-header')", "AppHeader = new (require('app-header'))", "AppHeader = new (require('headers').appHeader)"],
    invalid: [
      {
        code: "appHeader = new require('app-header')",
        errors: [
          {
            message: 'Unexpected use of new with require.',
            type: 'NewExpression'
          }
        ]
      },
      {
        code: "appHeader = new require('headers').appHeader",
        errors: [
          {
            message: 'Unexpected use of new with require.',
            type: 'NewExpression'
          }
        ]
      }
    ]
  });

}).call(this);
