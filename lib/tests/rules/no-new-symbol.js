// Generated by CoffeeScript 2.3.1
(function() {
  /**
   * @fileoverview Tests for the no-new-symbol rule
   * @author Alberto Rodríguez
   */
  'use strict';
  var RuleTester, rule, ruleTester;

  //------------------------------------------------------------------------------
  // Requirements
  //------------------------------------------------------------------------------
  rule = require('eslint/lib/rules/no-new-symbol');

  ({RuleTester} = require('eslint'));

  //------------------------------------------------------------------------------
  // Tests
  //------------------------------------------------------------------------------
  ruleTester = new RuleTester({
    parser: '../../..',
    env: {
      es6: true
    }
  });

  ruleTester.run('no-new-symbol', rule, {
    valid: ["foo = Symbol('foo')", "(Symbol) -> baz = new Symbol 'baz'", 'Symbol = ->\nnew Symbol()'],
    invalid: [
      {
        code: "foo = new Symbol('foo')",
        errors: [
          {
            message: '`Symbol` cannot be called as a constructor.'
          }
        ]
      },
      {
        code: "->\n  Symbol = ->\nbaz = new Symbol('baz')",
        errors: [
          {
            message: '`Symbol` cannot be called as a constructor.'
          }
        ]
      }
    ]
  });

}).call(this);
