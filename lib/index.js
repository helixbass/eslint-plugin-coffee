// Generated by CoffeeScript 2.3.1
(function() {
  var configureAsError, fflatten, ffromPairs, flow, fmap, fmapWithKey, parseForESLint, rules;

  ({
    flow,
    map: fmap,
    flatten: fflatten,
    fromPairs: ffromPairs
  } = require('lodash/fp'));

  fmapWithKey = fmap.convert({
    cap: false
  });

  ({parseForESLint} = require('./parser'));

  rules = flow(fmap(function(rule) {
    return [rule, require(`./rules/${rule}`)];
  }), ffromPairs)(['use-isnan', 'no-self-compare', 'no-eq-null', 'valid-typeof', 'no-negated-condition', 'yoda']);

  configureAsError = flow(fmapWithKey(function(_, rule) {
    return [[`coffee/${rule}`, "error"], [rule, "off"]];
  }), fflatten, ffromPairs);

  module.exports = {
    rules,
    configs: {
      all: {
        plugins: ['coffee'],
        parser: 'eslint-plugin-coffee',
        rules: configureAsError(rules)
      }
    },
    parseForESLint
  };

}).call(this);
