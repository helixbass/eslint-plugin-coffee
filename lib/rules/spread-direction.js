// Generated by CoffeeScript 2.5.0
(function() {
  /**
   * @fileoverview This rule enforces consistent usage of prefix or postfix `...`
   * @author Julian Rosse
   */
  'use strict';
  //------------------------------------------------------------------------------
  // Rule Definition
  //------------------------------------------------------------------------------
  module.exports = {
    meta: {
      docs: {
        description: "use postfix or prefix spread dots '...'",
        category: 'Stylistic Issues',
        recommended: false
      },
      // url: 'https://eslint.org/docs/rules/space-unary-ops'
      schema: [
        {
          enum: ['postfix',
        'prefix']
        }
      ]
    },
    create: function(context) {
      var check, ref, usePostfix;
      usePostfix = ((ref = context.options) != null ? ref[0] : void 0) === 'postfix';
      check = function(node) {
        if (!node.argument) {
          return;
        }
        if (node.postfix && !usePostfix) {
          return context.report({
            node,
            message: "Use the prefix form of '...'"
          });
        } else if (!node.postfix && usePostfix) {
          return context.report({
            node,
            message: "Use the postfix form of '...'"
          });
        }
      };
      return {
        //--------------------------------------------------------------------------
        // Public
        //--------------------------------------------------------------------------
        SpreadElement: check,
        RestElement: check,
        ExperimentalSpreadProperty: check,
        ExperimentalRestProperty: check,
        JSXSpreadAttribute: check
      };
    }
  };

}).call(this);
